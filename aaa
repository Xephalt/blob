.then(function(data) {
    el.dataset.pending = '0';
    el.dataset.liked = '1';          // ← verrouille définitivement
    el.style.pointerEvents = 'none'; // ← désactive tous les clics
    if (data && data.posts && data.posts[postId]) {
        if (countEl) countEl.textContent = data.posts[postId].total;
    }
})
